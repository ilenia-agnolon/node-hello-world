<img src="./boolean-logo.png" width="20px">

# node-hello-world

<img src="./node-hello-world.png" width="200px">

## 🇬🇧 English Version

### Overview

Node.js exercise focused on initializing a project, running scripts through npm, and enabling automatic file watching for live updates.

### Assignment Summary

The assignment required to:

- Initialize a new Node.js project using `npm init`.
- Create an `index.js` file that prints **“Hello World”** to the terminal and execute it using basic Node commands.
- Add a `"start"` script inside `package.json` to run the program with `npm run start`.
- Add a `"watch"` script that automatically reloads when files change.  
  Running `npm run watch`, modifying the code, and changing the output to **“Hello Boolean”** should update the terminal without restarting the server manually.

---

## 🇮🇹 Versione Italiana

### Panoramica

Esercizio Node.js dedicato alla creazione della prima applicazione, alla gestione degli script npm e all’utilizzo di un sistema di watch per aggiornare il codice in tempo reale.

### Riassunto della Consegna

La consegna richiedeva di:

- Creare un nuovo progetto Node.js con `npm init`.
- Scrivere un file `index.js` che stampi **“Hello World”** nel terminale, eseguendolo tramite i comandi base di Node.
- Impostare uno script `"start"` in `package.json` per avviare il programma con `npm run start`.
- Impostare uno script `"watch"` che ricarichi automaticamente l’applicazione quando si modificano i file.  
  Avviando `npm run watch` e modificando il codice per stampare **“Hello Boolean”**, il terminale deve aggiornarsi senza riavvio manuale del server.
